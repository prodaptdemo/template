vdus:
  VDU1:
    config:
      firewall: |
        package firewall
           config defaults
               option syn_flood '1'
           config rule
               option syn_flood '2'
            config defaults
               option syn_flood '1'
           config rule
               option syn_flood '2'
